data_root: data
dataset_name: steele
batch_size: 32
num_epochs: 30
learning_rate: 0.0005
weight_decay: 0.0005
latent_dim: 128
hidden_dim: 64
tcn_dropout: 0.3
num_classes: 4
eeg_channels: 28
esg_channels: 15
emg_channels: 8
num_workers: 2
seed: 42
device: cuda
use_focal_loss: true
focal_gamma: 2.0
save_dir: results
